<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Node</title>
    </head>
    <body>
        {{> header}}
        {{#if title}}
        <h1>{{title}}</h1>
        <p>{{description}}</p>
        <img src="{{img}}" alt="upload"/>
        {{/if}}
    </body>
</html>